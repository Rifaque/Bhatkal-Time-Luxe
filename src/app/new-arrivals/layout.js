export const metadata = {
  title: 'New Arrivals',
  description: 'Discover the latest additions to our luxury watch collection at Bhatkal Time Luxe.',
};

export default function NewArrivalsLayout({ children }) {
  return children;
}
