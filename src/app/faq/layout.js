export const metadata = {
  title: 'FAQ',
  description: 'Frequently asked questions about Bhatkal Time Luxe — shipping, returns, authenticity, and more.',
};

export default function FAQLayout({ children }) {
  return children;
}
