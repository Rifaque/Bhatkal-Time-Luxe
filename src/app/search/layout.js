export const metadata = {
  title: 'Search Watches',
  description: 'Search our full collection of premium luxury timepieces at Bhatkal Time Luxe.',
  robots: { index: false },
};

export default function SearchLayout({ children }) {
  return children;
}
